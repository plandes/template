#set($author-latex-phone = $author-phone.replace("-", "--"))
\documentclass{letter}

%% options:
% logo[=<filename>.eps]: add a logo (default large-bw); don't add the extension
% logosize[=size]: the size of the logo, defaults to 1.5in
% header: add a header at top and under the logo (if one defined)
% signature[=<filename>.eps]: add a signature (no ext) default: signature.eps

% personal
\usepackage[signature]{zenletter}
% business
%\usepackage[logo,logosize=1in,header,signature]{zenletter}

\zlinitpersonal{${author-name}}
{${organization-address}}
{${organization-city}}
{${organization-state}}
{${organization-zip}}
{${author-latex-phone}}
{${author-email}}

%\zlinitorg{${author-name}}
%{${organization-name}}
%{${organization-address}}
%{${organization-city}}
%{${organization-state}}
%{${organization-zip}}
%{${author-latex-phone}}
%{${author-email}}

\begin{document}

\begin{letter}{Some Department\\
12345 Some St,\\
Somecity, SS. 12345-1150}

\opening{To whom it may concern,}

I certify that I, \fromname, am indeed, writing this letter.

%\zlclosing{Sincerely,}
\closing{Sincerely,}

\end{letter}
\end{document}
