;;; package --- Summary
;;; Commentary:
;;
;; Unit tests of ${project-name}-test.el
;;
;;; Code:

(require 'ert)
(require 'dash)
(require '${project-name})

(ert-deftest test-load ()
  "Test successful evaluation of ${project-name}"
  (should nil))

(provide '${project-name}-test)

;;; ${project-name}-test ends here
