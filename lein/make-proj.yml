project:
  name: Zensol Clojure Project
  description: Simple Clojure project designed to work with [Zensol Build](https://github.com/plandes/clj-zenbuild).
  template-directory:
    description: root of source code tree to interpolate
    example: view/template/proj
    default: make-proj
  generate:
    excludes:
      - LICENSE$
  context:
    project:
      description: github repo name
      example: clj-nl-parse
      default: clj-someproj
    project-name:
      description: very short few word project description
      default: WRITE ME
      example: Some useful project
    project-description:
      description: a short project descsripion
      default: WRITE ME
      example: This library provides generalized tooling library.
    group:
      description: maven group coordinate
      example: com.zensols.nlp
      default: com.zensols.tools
    artifact:
      description: maven artifact coordinate
      example: parse
      default: someproj
    package:
      description: root package space (creates <package>.core)
      example: zensols.nlp
      default: zensols.tools
    user:
      description: github user name
      example: plandes
    app-name:
      description: script name used with appassembler
      example: nlparser
      default: someproj
